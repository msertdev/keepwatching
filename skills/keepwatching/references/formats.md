# The format library

Generated by `kw site build` from `formats/*/meta.yml` and `data.yml`. Do not edit by hand.

24 formats · 0 measured · 0 samples total

`n` is the number of published videos behind a row. `n = 0` means untested —
a valid state, not a gap to paper over.

## cold-open

| Format | n | avg viewed | hook @3s | Hypothesis |
|---|---|---|---|---|
| `cold-open-line` | 0 | — | — | A single sentence with no title card, no logo and no build-up removes every reason to swipe in the first second; the cost of reading it is lower than the cost of scrolling past. |
| `cold-open-question` | 0 | — | — | A question the viewer cannot immediately answer opens a loop, and the two-second silence before the partial answer is long enough to feel like a pause rather than a delay. |
| `pattern-interrupt` | 0 | — | — | Asserting something flatly and then contradicting it two seconds later breaks the sentence the viewer was already finishing in their head, and a broken prediction restarts attention. |

**`cold-open-line`** — Cold open — one line  
Use when: The sentence is strong enough to carry the clip alone.  
Avoid when: The sentence needs a subject line to make sense.  
9s · 30fps · `cold-open-line@1.0.0+385dab9ddcbe`

**`cold-open-question`** — Cold open — question  
Use when: The question has a specific, checkable answer.  
Avoid when: The question is rhetorical — an unanswerable question closes the loop instantly.  
10s · 30fps · `cold-open-question@1.0.0+b497e2f26cb8`

**`pattern-interrupt`** — Pattern interrupt  
Use when: The common assumption is confidently wrong and you can prove it.  
Avoid when: The reversal is a technicality — the viewer will feel tricked, not corrected.  
10s · 30fps · `pattern-interrupt@1.0.0+e534ecb405ee`

## comparison

| Format | n | avg viewed | hook @3s | Hypothesis |
|---|---|---|---|---|
| `bar-race` | 0 | — | — | Two bars growing at different speeds make the gap visible before it is stated, so the viewer reaches the conclusion a beat before the caption confirms it. |
| `split-compare` | 0 | — | — | Two panels shown together force a choice, and the delayed right-hand panel makes the viewer commit to a guess before the answer arrives. |
| `split-verdict` | 0 | — | — | Naming a winner explicitly at the end resolves the tension the split opened; without a verdict the viewer leaves at the moment both panels are visible. |

**`bar-race`** — Bar race — two lanes  
Use when: The difference between two quantities is large and worth watching accumulate.  
Avoid when: The two values are close — the race will look like a tie and land as nothing.  
12s · 30fps · `bar-race@1.0.0+c19d848deb0e`

**`split-compare`** — Split comparison  
Use when: Two options differ on one number the audience will find unintuitive.  
Avoid when: The two things differ on many dimensions — a split will oversimplify them.  
12s · 30fps · `split-compare@1.0.0+1c2b56402770`

**`split-verdict`** — Split — verdict  
Use when: The audience expects you to take a side.  
Avoid when: The honest answer is 'it depends' — a forced verdict will cost you credibility.  
11s · 30fps · `split-verdict@1.0.0+05d70a279ccf`

## countdown

| Format | n | avg viewed | hook @3s | Hypothesis |
|---|---|---|---|---|
| `countdown-clock` | 0 | — | — | A visible clock running to zero creates a deadline the viewer did not choose, and leaving before it hits zero feels like abandoning something already started. |
| `countdown-list` | 0 | — | — | Numbering the items down to one tells the viewer exactly how much is left, which converts an open-ended list into a finite commitment they will complete. |

**`countdown-clock`** — Countdown — clock  
Use when: You have a payoff worth waiting for and the clip is under 15 seconds.  
Avoid when: There is no payoff at zero — the format writes a cheque the ending must cash.  
12s · 30fps · `countdown-clock@1.0.0+f007453f6c9e`

**`countdown-list`** — Countdown — 5 to 1  
Use when: You have between three and six discrete items and the last one is the best.  
Avoid when: The items are equally weighted — descending order will make the ending feel flat.  
14s · 30fps · `countdown-list@1.0.0+b46d031d4ae4`

## escalation

| Format | n | avg viewed | hook @3s | Hypothesis |
|---|---|---|---|---|
| `escalation-counter` | 0 | — | — | A counter that passes named thresholds converts a single climbing number into a series of small events, each one a fresh reason not to leave yet. |
| `escalation-ladder` | 0 | — | — | Each row larger than the last teaches the viewer that the next one is bigger again, so the expectation of a bigger number carries them to the final row. |

**`escalation-counter`** — Escalation — threshold counter  
Use when: The number crosses milestones the audience already recognises.  
Avoid when: There are no meaningful thresholds — the labels will feel arbitrary.  
13s · 30fps · `escalation-counter@1.0.0+3f95dd12b339`

**`escalation-ladder`** — Escalation ladder  
Use when: The values genuinely escalate and the last one is dramatic.  
Avoid when: The escalation flattens out — the format promises a peak it must deliver.  
13s · 30fps · `escalation-ladder@1.0.0+bf4bca9b75a6`

## myth-fact

| Format | n | avg viewed | hook @3s | Hypothesis |
|---|---|---|---|---|
| `myth-vs-fact` | 0 | — | — | Stating a belief the viewer probably holds triggers a defensive read, and defensiveness is attention: they stay to see whether they are the one being corrected. |
| `three-myths` | 0 | — | — | Three corrections in sequence give the viewer three chances to be personally wrong, and each miss raises the odds they stay for the next one. |

**`myth-vs-fact`** — Myth vs fact  
Use when: The myth is genuinely widespread and the correction is well-sourced.  
Avoid when: You cannot source the correction. This format converts confidence into belief.  
12s · 30fps · `myth-vs-fact@1.0.0+e9b83a013a90`

**`three-myths`** — Three myths  
Use when: You have three unrelated corrections in the same subject area.  
Avoid when: One of the three is weak — the weakest item sets the credibility of all three.  
13s · 30fps · `three-myths@1.0.0+5a61507bc12b`

## progress

| Format | n | avg viewed | hook @3s | Hypothesis |
|---|---|---|---|---|
| `checklist-run` | 0 | — | — | Items ticking off on a fixed cadence set a rhythm the viewer can predict, and a predictable rhythm carries attention through the middle where most formats lose it. |
| `progress-bar` | 0 | — | — | A bar with labelled milestones tells the viewer both how far they are and what they have not seen yet, so the decision to leave is made against a visible cost. |

**`checklist-run`** — Checklist run  
Use when: You have four to eight parallel items with no ordering.  
Avoid when: The items build on each other — a checklist implies they are independent.  
13s · 30fps · `checklist-run@1.0.0+270cf9fdb100`

**`progress-bar`** — Progress bar  
Use when: The clip has genuine stages and the last stage is worth reaching.  
Avoid when: There are no real stages — a fake progress bar is the fastest way to lose trust.  
12s · 30fps · `progress-bar@1.0.0+63ae33a6b1db`

## ranking

| Format | n | avg viewed | hook @3s | Hypothesis |
|---|---|---|---|---|
| `ranking-reveal` | 0 | — | — | Showing rank 1 immediately trades suspense for clarity: the viewer learns the answer in the first two seconds and stays only if the rest of the list surprises them. |
| `ranking-suspense` | 0 | — | — | Withholding rank 1 until the final second converts the whole clip into a single unanswered question, trading early drop-off for a higher completion rate among those who stay. |
| `tier-list` | 0 | — | — | Tier labels invite disagreement, and a viewer composing an objection in their head watches to the end to check whether their pick was ranked. |

**`ranking-reveal`** — Ranking — top down  
Use when: The headline is the ranking itself and you want shares over watch-through.  
Avoid when: You are optimising for completion — this format gives away its own ending.  
12s · 30fps · `ranking-reveal@1.0.0+0240a4bc4679`

**`ranking-suspense`** — Ranking — bottom up  
Use when: Completion and replays matter more than immediate shares.  
Avoid when: The audience already knows the likely #1 — the withholding will read as padding.  
13s · 30fps · `ranking-suspense@1.0.0+7101b32c10da`

**`tier-list`** — Tier list  
Use when: The audience has strong opinions and a comment section worth provoking.  
Avoid when: The subject is factual — tiers imply judgement the data may not support.  
13s · 30fps · `tier-list@1.0.0+48744086f377`

## reveal

| Format | n | avg viewed | hook @3s | Hypothesis |
|---|---|---|---|---|
| `before-after` | 0 | — | — | A wipe that moves across the frame is a change the eye must track, and tracking a moving edge is harder to abandon mid-motion than reading a static slide. |
| `redacted-reveal` | 0 | — | — | A visibly hidden word is a question the viewer cannot look away from, and the hiding costs nothing to read, so the drop-off that usually happens at second three is deferred to the uncover. |
| `reverse-reveal` | 0 | — | — | Leading with the answer and withholding the question inverts the usual contract: the viewer stays to find out what they have just been told, not what they are about to be. |

**`before-after`** — Before / after wipe  
Use when: You can show the same subject in two states.  
Avoid when: The two states look similar — the wipe will read as a glitch.  
10s · 30fps · `before-after@1.0.0+c7b6e1ae5d17`

**`redacted-reveal`** — Redacted reveal  
Use when: One word or number is the entire payload.  
Avoid when: The hidden thing is guessable in under a second — the format will feel like a trick.  
10s · 30fps · `redacted-reveal@1.0.0+ad7a5f6d43ce`

**`reverse-reveal`** — Reverse reveal  
Use when: The answer is strange enough to be its own hook.  
Avoid when: The answer needs context to even parse — leading with it will read as noise.  
11s · 30fps · `reverse-reveal@1.0.0+e0ee41c0930a`

## reverse

| Format | n | avg viewed | hook @3s | Hypothesis |
|---|---|---|---|---|
| `loop-seam` | 0 | — | — | Ending on the frame the clip opened with removes the visual signal that it is over, so the platform's autoplay loop reads as a continuation and counts a second view. |

**`loop-seam`** — Loop seam  
Use when: The clip is short and its content is genuinely circular.  
Avoid when: The clip has a real ending — hiding it will frustrate rather than loop.  
8s · 30fps · `loop-seam@1.0.0+2407eba62732`

## stat-counter

| Format | n | avg viewed | hook @3s | Hypothesis |
|---|---|---|---|---|
| `stat-counter-context` | 0 | — | — | Holding the number alone for four seconds builds a question the viewer cannot answer, so the context line at 4.5s pays off a debt rather than adding a fact. |
| `stat-counter-rise` | 0 | — | — | A number that is visibly still moving reads as unfinished, so the first three seconds cost the viewer nothing to stay for; the bar underneath tells them how long that costs. |
| `stat-counter-stack` | 0 | — | — | Icons filling in at the same rate as the digits turn an abstract number into a countable pile, holding attention past the point where a bare number stops being new. |

**`stat-counter-context`** — Stat counter — context flip  
Use when: The number is meaningless without a comparison, and the comparison is the actual payload.  
Avoid when: The number already carries its own meaning — the pause will feel like stalling.  
11s · 30fps · `stat-counter-context@1.0.0+b719dc8b602e`

**`stat-counter-rise`** — Stat counter — rise  
Use when: You have one number that is large, surprising, and easy to say out loud.  
Avoid when: The number needs a paragraph of setup before it means anything.  
10s · 30fps · `stat-counter-rise@1.0.0+c91af1b2e218`

**`stat-counter-stack`** — Stat counter — stacking proof  
Use when: The number counts discrete things a viewer can picture one at a time.  
Avoid when: The quantity is continuous (money, time, mass) — the icons will read as decoration.  
12s · 30fps · `stat-counter-stack@1.0.0+1a6596dde9b9`
